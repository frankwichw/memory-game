import React from "react";
import './NameDiv.css';

const NameDiv = props => (
    <div className="jumbotron">
        <h1>Clicky Game!</h1>
        <h2>Click on a tile to begin, don't click on the same one twice!</h2>
    </div>
  );
  
  export default NameDiv;